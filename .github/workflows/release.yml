name: Release

on:
  push:
    branches: [main]

permissions: {}

jobs:
  example:
    uses: equinor/ops-actions/.github/workflows/release-please.yml@v9.26.1
    with:
      release_type: python
    permissions:
      contents: write
      issues: write
      pull-requests: write
